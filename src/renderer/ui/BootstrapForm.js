import Component from "../core/Component.js";

class BootstrapForm extends Component{
     constructor(){
     	this.inline = false; //是否是行表格
     	this.rows = [];
     	this.children = []; //添加的所有界面子元素
     }
     emitCode(){

     }
     addElement(ele){ //添加子元素

     }
     removeElement(ele){ //删除子元素

     }
}